# M13.4 Verification

Expected release gates:

- M07 → M13.4 source contracts: PASS
- PHP syntax: PASS
- Mission Tower Prettier: PASS
- Mission Tower ESLint excluding the known offline `import/order` native resolver: PASS
- TypeScript: only historical Wayfinder generated imports may remain
- Coolify topology unchanged: app + PostgreSQL 17/pgvector + Redis
- packaged dependency directories: 0
- font files: 0
- real secret values: 0

Environment-dependent probes remain separate:

```bash
php artisan test --filter=MissionTowerChatTest
npm run build
```

If `vendor/autoload.php` or the optional native Rolldown binding is absent, classify the probe as BLOCKED rather than PASS/FAIL for M13.4.
